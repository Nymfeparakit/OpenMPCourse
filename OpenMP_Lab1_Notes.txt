Матрица из MxN натуральных чисел (задаются случайно)
Количество семерок в записи числа всех попарных сумм элементов для каждой строки
Измерить время выполнения для различных значений параметров
Посмотреть в диспетчере число используемых потоков

7. Выполнить для разных параметров, занести результаты в таблицу+
6. Размер матрицы определять из аргументов консоли+
8. 

Если дать двум потокам одну строку, то тогда они будут работать с общими данными! 
Поэтому одна строка должна отдаваться одному потоку

Считать факториал от количества столбцов - трудозатратратно, так как кол-во столбцов может быть большим
Вложенность 4 цикла - тоже плохо
Как можно избежать такой вложенности?

http://jakascorner.com/blog/2016/06/omp-for-scheduling.html - For & Scheduling

Контрольные вопросы

1.	В чем состоят основы технологии OpenMP? 

Технология OpenMP (Open Multi-Processing) позволяет разделять код на несколько потоков, в специально указываемых при помощи директив местах кода – параллельных фрагментах.
В технологии OpenMP, потоки одной и той же параллельной программы выполняются в общем адресном пространстве, что обеспечивает возможность использования общих данных для параллельно выполняемых потоков.

2.	В чем состоят основные преимущества и недостатки технологии OpenMP? 

+Общее адресное пространство

3.	Что понимается под параллельной программой в рамках технологии OpenMP? 

Содержащая параллельные фрагменты???

4.	Какие проблемы возникают при использовании общих данных в параллельно выполняемых потоках? 

5.	Какой формат записи директив OpenMP? 

#pragma omp for/parallel опции num_threads()

6.	В чем состоит назначение директивы parallel? 

Фрагмент, отмеченный директивой выполняется несколькими потоками паралллельно

7.	В чем состоят понятия фрагмента, области и секции параллельной программы? 

Фрагмент(parallel construct) - то, что отмечено директивой parallle; область (parallel region)параллельное выполняемые участки кода в функциях, которые вызываются из параллльеных фрагментов
Фрагмент, отмеченные директивой sections. Тело блока - последовательность секций, каждая из секций выполняется только один раз одним потоком из группы
8.	Как осуществляется распараллеливание циклов в OpenMP? Какие условия должны выполняться, чтобы циклы могли быть распараллелены? 

Итерации распределяются между потоками, при этом между итерациями не должно быть информационной зависимости
Цикл должен иметь особый вид - должен иметь переменную счетчик
9.	Какие возможности имеются в OpenMP для управления распределением итераций циклов между потоками? 

С помощью параметра schedule можно управлять распределением итераций
static - итерации делятся поровну
dynamic - до начала выполнения потокам выделяется chunk, затем - кто как успел
guided - то же, что в runtime, но размер порций уменбшается. Размер пропорционален количеству итераций / число потоков The minimum size of a chunk is set by chunk-size
runtime - в соответствии с переменной окружения runtime 
10.	Как определяется порядок выполнения итераций в распараллеливаемых циклах?
